# I18n Content Split Implementation Plan

> **For agentic workers:** Execute inline in this session (user requested immediate start).

**Goal:** Split content by language directories, filter UI by language, rewrite SeaTunnel article, add AI rules docs.

**Architecture:** Scanner emits `lang` on every meta/search item; pages filter by `useAppContext().language`; EN tree is empty scaffold.

**Tech Stack:** Vite, React, existing `scripts/content-scanner.mjs`, Vitest.

## Global Constraints

- Language dirs: `content/zh`, `content/en` only
- Strict isolation (no fallback)
- Same slug across languages when paired
- Do not commit unless user asks
- Redact secrets in published Markdown

---

### Task 1: Migrate content + rewrite SeaTunnel article

**Files:**
- Move: `content/{articles,banks,questions}` → `content/zh/...`
- Create: `content/en/{articles,banks,questions}/.gitkeep` + README
- Create: `content/zh/articles/seatunnel-data-sync.md` (rewrite; delete 数据同步解决方案.md)
- Update: `content/README.md`

- [ ] Migrate directories
- [ ] Rewrite article with frontmatter, redact passwords
- [ ] Update content README

### Task 2: Scanner + types + content helpers

**Files:**
- Modify: `scripts/content-scanner.mjs`
- Modify: `src/types/content.ts`
- Modify: `src/data/content.ts`, `src/data/searchCorpus.ts`
- Create: `src/lib/localizedContent.ts`
- Modify tests

- [ ] Scan `content/{lang}/...`, attach `lang`, unique ids `*-${lang}-${slug}`
- [ ] Glob `content/{zh,en}/**/*.md`
- [ ] `getLocalized*` helpers + loaders take `language`

### Task 3: Wire pages to language filter

**Files:** HomePage, ArticleListPage, ArticleDetailPage, QuestionListPage, QuestionDetailPage, TagsPage, SearchPage

- [ ] Filter lists/search by language
- [ ] Detail find by slug+lang; missing → notFound

### Task 4: AI content rules

**Files:**
- Create: `docs/ai-content-rules/RULES.zh.md`
- Create: `docs/ai-content-rules/RULES.en.md`

- [ ] Document frontmatter, dirs, slug, redact, bilingual rules

### Task 5: Verify

- [ ] `npm test` passes
- [ ] `npm run build` succeeds
