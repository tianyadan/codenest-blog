---
title: "Spring 中的 DI 是什么？"
description: "Spring知识库 · 原站真题整理"
tags: ["Spring","DI"]
difficulty: medium
source: "面试鸭"
---

## 答案

DI （Dependency Injection）依赖注入，是 Spring 框架用于实现控制反转（IOC）的**一种机制**。DI 的核心思想是由容器负责对象的依赖注入。
通过 DI，Spring 容器在创建一个对象时，会**自动**将这个对象的依赖注入进去，避免手动创建，这样可以让对象与其依赖的对象解耦，提升系统的灵活性和可维护性。
优点：
1. 解耦合：通过将对象的依赖从代码中抽离出来，由容器负责管理，降低类之间的耦合度。
2. 提高测试性：通过注入 mock 对象或替代实现，DI 使得单元测试更加容易。
3. 提高可维护性和灵活性：通过配置文件或注解，开发者可以在不修改代码的情况下更改依赖，增加应用程序的可扩展性和灵活性。