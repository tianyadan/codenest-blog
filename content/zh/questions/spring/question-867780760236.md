---
title: "Spring MVC 详细工作原理？"
description: "Spring知识库 · 原站真题整理"
tags: ["Spring","MVC"]
difficulty: medium
source: "面试鸭，ChatGPT"
---

## 答案

1. **客户端请求**：用户发送 Http 请求，DispatcherServlet 接收请求。
2. **执行拦截器的preHandle()**：如果配置了拦截器，Spring MVC 会首先执行 preHandle()方法，如果返回 false，请求处理终止，否则继续处理。
3. **请求映射**：DispatcherServlet 通过 HandlerMapping 找到对应的 HandlerExecutionChain(这里面包含了很多定义的HandlerInterceptor 拦截器)。然后通过 HandleAdapter 适配器的适配（适配器模式）后执行 handler，即通过 controller 调用。
4. **执行控制器方法**：如果返回视图(JSP、Thymeleaf)，则执行试图解析器进行视图解析和渲染，如果返回的是对象如（JSON），则通过消息转换器如（MappingJackson2HttpMessageConvert） 将对象转为 JSON 数据。
5. **执行拦截器的postHandle() **：如果返回视图，拦截器的 postHandle()方法会在视图渲染之前执行。对于 JSON 的响应，仍然会执行。
6. **视图渲染或 JSON 响应**：对于视图，视图解析器会解析视图名称，渲染成 html 页面，对于 JSON 格式响应，Spring MVC 会将对象转为 JSON，并返回给客户端。
7. **执行拦截器的 afterCompletion()**: 视图渲染或 JSON 响应完成后，会调用拦截器的afterCompletion()方法清理资源或记录日志。
8. **响应客户端**：最终将渲染的视图或 JSON 数据格式返回给客户端。