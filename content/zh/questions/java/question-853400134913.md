---
title: "什么是 Java 泛型的上下界限定符？"
description: "Java知识库 · 原站真题整理"
tags: ["Java"]
difficulty: medium
---

## 答案

Java 泛型的上下界限定符 用于泛型类型参数进行范围限制，主要有上界限定符（Upper Bround Wildcards） 和下界限定符（Lower Bound Wildcards）。

1. 上界限定符（？ extends T）:
 - ? extends T 表示通配符类型必须是 T 类型或 T 的**子类**。
 - 作用：允许使用 T 或其子类型作为泛型参数，通常用于读取操作，确保可以读取为 T 或 T 的子类的对象。

2. 下界限定符(? super T)
- 定义： ? super T 表示通配符类型必须是 T 类型或 T 的**父类**。
- 作用： 允许使用 T 或其父类型作为泛型参数，通常用于写入操作，确保可以安全地向泛型集合中插入 T 类型的对象。

**总结 上界 限定子类 ，下界 限定父类。**