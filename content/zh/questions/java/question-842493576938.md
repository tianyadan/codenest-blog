---
title: "什么是 Java 的 Integer 缓存池？"
description: "Java知识库 · 原站真题整理"
tags: ["Java"]
difficulty: medium
---

## 答案


Integer **缓存池概念**

Java 的 Integer 缓存池(Integer Cache) 是为了提升性能和节省内存。根据实践发现大部分的数据操作都集中在值比较小的范围，因此缓存这些对象可以减少内存分配和垃圾回收的负担，提升性能。

在 -128 到 127 范围内的 Integer 对象会被缓存和复用。

**原理：**

Java 在自动装箱时，对于 -128 到 127 之间的 int 类型，会直接返回一个已经缓存的 Integer 对象，而不是创建新的对象。

**使用场景：**

- 自动装箱：当基本类型int 转为包装类Integer 时，若数值在患处范围内，返回缓存对象。
- 值比较：由于相同范围内的整数使用同一个缓存对象，使用 == 可以正确比较它们的地址（引用相同），而不需要使用 equals() 。 但要注意对于超过缓存范围内的 Integer 对象，== 比较的是对象引用，而不是数值，数值比较应该使用 equals()方法。