---
title: "BigDecimal 为什么能保证精度不丢失？"
description: "Java知识库 · 原站真题整理"
tags: ["Java"]
difficulty: medium
---

## 答案

BigDecimal  能够保证精度，是因为它使用了任意精度的整数表示法，而不是浮动的二进制表示。

BigDecimal 内部使用两个字段存储数字，一个是整数部分 intVal , 另一个是用来表示小数点的位置 scale ，避免了浮点数转化过程中可能的精度丢失。

计算时通过整数计算，再结合小数点位置和设置的精度与舍入行为，控制结果精度，避免了由默认浮点数舍入导致的误差。

举个例子：

0.2 * 0.2  =0.04

 因为使用 BigInteger 来精确存储数字，对于0.1,0.2 BigDecimal 会将他们存储为整数（去除小数点）并通过scale ( 小数点的位置）来跟踪小数点的位置。