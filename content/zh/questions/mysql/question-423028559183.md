---
title: "MySQL 中 EXPLAIN 里的 type 字段与 ref、range 有什么区别？"
description: "MySQL知识库 · 原站真题整理"
tags: ["MySQL"]
difficulty: easy
---

## 答案

ref 是等值匹配，比如 `where name = 'zhang'`，直接定位到某个值；range 是范围扫描，比如 `where id > 100 and id <200 `，需要扫描一段连续的索引区间。性能上 ref 通常比 range 好 ，因为等值匹配定位更精准，range 还是得沿着叶子链表扫描一边。