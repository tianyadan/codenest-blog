---
title: "MVCC 能不能解决写写冲突？"
description: "MySQL知识库 · 原站真题整理"
tags: ["MySQL","MVCC"]
difficulty: easy
---

## 答案

**不能**，m_ids 只记录了生成 ReadView 那一刻的活跃事务，但事务 ID 是递增的，之后新启动的事务 ID 肯定比 m_ids 里的都大。如果只用 m_ids 判断，新启动事务修改的数据可能被当成已提交的看到。max_trx_id 就是来挡住这些后来者的，trx_id > = max_trx_id 一律不可见。