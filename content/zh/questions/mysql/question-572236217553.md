---
title: "MySQL 中如何判断优化器是否选错索引？"
description: "MySQL知识库 · 原站真题整理"
tags: ["MySQL","索引"]
difficulty: easy
---

## 答案

可以使用 `FORCE INDEX` 强制指定某个索引跑一遍，对比执行时间。如果强制走某个索引反而快很多，说明优化器确实选错了。另外可以用 `ANALY TABLE` 更新统计信息，让优化器重新算成本。 MySQL 8.0 还可以用 `EXPLAIN ANALYZE` 看实际执行的行数和预估行数差多少，差太大说明统计信息不准。