---
title: "Synchronized 和 ReentrantLock 有什么区别？"
description: "Java 并发知识库 · 原站真题整理"
tags: ["Java 并发"]
difficulty: medium
---

## 答案

Synchronized 是 Java **内置的关键字**，实现基本的同步机制，**不支持超时**，**非公平**，**不可中断**，**不支持多条件**。
ReentrantLock 是 **JUC 类库提供**的，由 JDK 1.5 引入，支持**设置超时时间**，**可以避免死锁**，**比较灵活**，并且**支持公平锁**，**可中断**，**支持多条件判断**。

**ReentrantLock 需要手动解锁，而 Synchronized 不需要，他们都是可重入锁。**

一般情况下用 Synchronized 足够了，比较简单，而 ReentrantLock 比较灵活，支持的功能也比较多，所以复杂情况下用 ReentrantLock。


**总结表格：**


|\ |synchronized | ReentrantLock |
| --- |--- | --- |
|  **超时**| 不支持 |支持|
|  **公平**| 非公平 | 支持公平锁|
|  **中断**|不可中断 |可中断 |
|  **多条件**| 不支持 |支持 |




