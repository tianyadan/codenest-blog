---
title: "Java 中的 InheritableThreadLocal 是什么？"
description: "Java 并发知识库 · 原站真题整理"
tags: ["Java 并发"]
difficulty: medium
---

## 答案

**InheritableThreadLocal** 是 **ThreadLocal** 的一个**扩展**，用于在线程创建时将父线程的 ThreadLocal 变量副本传递给子线程，使得子线程可以访问父线程中设置的本地变量。它解决了 ThreadLocal 无法在子线程中继承父线程本地变量的问题。

工作原理：

- 当创建子线程时，InheritableThreadLocal 的值会自动拷贝到子线程中。
- 子线程可以修改自己的副本，但不会影响父线程的值。

